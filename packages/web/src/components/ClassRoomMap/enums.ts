/** TIPO DI STRUMENTO IN USO */
export enum ToolType {
    POINTER,
    ADD,
    REMOVE,
    SWAP
}
/** TIPO DI SISTEMA DI RIFERIMENTO */
export enum ReferenceSystemType {
    CANVAS,
    GRID,
    SCREEN
}

export enum Orientation { N, E, S, W }