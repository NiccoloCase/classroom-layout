overwrite: true
schema:
  - "http://localhost:5000/graphql"
#documents: "src/graphql/**/*.ts"
generates:
  src/generated/graphql.tsx:
    config:
      noNamespaces: true
      withHooks: true
  
    plugins:
    - typescript
    - typescript-operations
